{{#if ok_msg}}

    <div class="container">
        <div class="alert alert-success">
        <strong>{{ok_msg}}</strong> 
        </div>
   </div>
  
{{/if}}

{{#if fail_msg}}
    <div class="container">
        <div class="alert alert-danger">
        <strong>{{fail_msg}}</strong> 
    </div>  
</div>
{{/if}}